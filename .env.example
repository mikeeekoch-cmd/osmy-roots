# Copy to .env.local. Never commit real credentials or paste them into chat.
# Intended names for the first implementation; no app is running yet.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-6-astra
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Optional privileged server operations only. Prefer user-scoped access with RLS.
SUPABASE_SECRET_KEY=
APP_URL=http://localhost:3000
# Developer context location; never expose in the UI or commit the private URL.
OSMY_CONTEXT_FOLDER_URL=
