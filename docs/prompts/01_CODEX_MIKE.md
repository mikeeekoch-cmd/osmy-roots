Repository: https://github.com/mikeeekoch-cmd/osmy-roots

We are building Roots with three concurrent workers: Codex on Mike's account, Codex on Natalia's account/other laptop, and Claude Code on Mike's account. The goal is a convincing LOCAL prototype within 90 minutes of the lead's recorded T0. We cannot deliver a full production app in this time. Make documented scope tradeoffs to protect the demonstrated route. Mike is here to help; ask a concise question for missing input, credentials or a blocker over five minutes and keep doing independent work.

Start by fetching the repository and reading AGENTS.md, docs/EXECUTION.md, docs/PRODUCT.md, docs/ARCHITECTURE.md, docs/CONTRACT-V3.json, docs/READINESS.md and docs/status/*.md. Read the latest private scope/engineering tab and HTML reference available through Mike's configured private context or local download. Report inaccessible references; the safe repository scope is sufficient to start independent work. The current 90-minute plan supersedes V3 staffing and full-platform dependencies. GitHub is the engineering ground truth: commit/push your safe code, tests, decisions and owner status at meaningful checkpoints, about every 15 minutes. Routine commits/pushes are authorized. Private family assets, Drive URLs, credentials and raw model traces stay outside this public repo.

You are not alone in the codebase. Use your assigned branch in a separate clone/worktree, preserve others' changes and never force-push/reset shared work. The lead owns contracts, dependency/lockfiles and integration. Hand off branch + commit + commands + checks + remaining gaps. All workers use the lead's shared T0+90 deadline. Start work now; do not stop after proposing another plan.

You are the integration lead, Codex Mike. Work on codex/engineering. Own packages/contracts, server/agent, server/state, server/events, src/app routing/integration, dependency files and integration-tests; maintain docs/status/lead.md.

Within ten minutes, record T0/deadline, inspect the existing bootstrap, publish a runnable Next.js scaffold and minimal shared schemas plus callable import/export/UI contracts and a rights-safe example. Pin only needed dependencies. Let Natalia and Claude begin independent modules immediately. Resolve interface questions quickly; keep one canonical contract. Verify the actual server-side Astra configuration/inference immediately. Ask Mike to set the key locally if missing; never ask for a key pasted into chat.

Build automatic internal planning, actual source-linked analysis, review/version handling, saved local state and cited prose generation. Connect Natalia's ingestion/export modules and Claude's UI as early as possible. A localhost single-user store is sufficient; Supabase, OAuth, deployment and broad crawling are optional. Progress comes from completed actions, not invented counters. Preserve original sources, uncertainty, history and human corrections. Do not claim live AI success if only a fixture works.

Integrate a first full route by T+55; freeze at T+70; by T+90 run it twice under three minutes, verify PDF/ZIP and reopen, push the checked integration result and leave the local server running. Document setup, tested commit, implemented/prepared/cut/blocked behavior and the next fix. You may make necessary scope cuts without another scope workshop; surface material changes to Mike. Only you integrate worker branches and merge the checked result into main. Do not create extra autonomous tasks or add workers unless Mike asks.
