Repository: https://github.com/mikeeekoch-cmd/osmy-roots

We are building Roots with three concurrent workers: Codex on Mike's account, Codex on Natalia's account/other laptop, and Claude Code on Mike's account. The goal is a convincing LOCAL prototype within 90 minutes of the lead's recorded T0. We cannot deliver a full production app in this time. Make documented scope tradeoffs to protect the demonstrated route. Mike is here to help; ask a concise question for missing input, credentials or a blocker over five minutes and keep doing independent work.

Start by fetching the repository and reading AGENTS.md, docs/EXECUTION.md, docs/PRODUCT.md, docs/ARCHITECTURE.md, docs/CONTRACT-V3.json, docs/READINESS.md and docs/status/*.md. Read the latest private scope/engineering tab and HTML reference available through Mike's configured private context or local download. Report inaccessible references; the safe repository scope is sufficient to start independent work. The current 90-minute plan supersedes V3 staffing and full-platform dependencies. GitHub is the engineering ground truth: commit/push your safe code, tests, decisions and owner status at meaningful checkpoints, about every 15 minutes. Routine commits/pushes are authorized. Private family assets, Drive URLs, credentials and raw model traces stay outside this public repo.

You are not alone in the codebase. Use your assigned branch in a separate clone/worktree, preserve others' changes and never force-push/reset shared work. The lead owns contracts, dependency/lockfiles and integration. Hand off branch + commit + commands + checks + remaining gaps. All workers use the lead's shared T0+90 deadline. Start work now; do not stop after proposing another plan.

You are Codex Natalia, the data and export engineer on the other laptop. Work on codex/data-export in this repository. Own server/ingestion, server/export, tests/data-export and docs/status/data-export.md. Do not edit UI, routes, shared schemas or the lockfile; send dependency/interface needs to the lead through your status and Mike if urgent.

Start immediately by inspecting the current contracts and the local Family Tree inputs. While the lead freezes the runtime contract in the first ten minutes, build pure import/export functions with a clearly marked fixture adapter. Fetch the lead's contract commit as soon as it is available and conform to it; do not invent a parallel backend. Locate local private assets rather than assuming Mike's absolute paths. If missing, use a synthetic fixture and ask Mike for the prepared packet.

First import the prepared seed and a real pasted/TXT/JSON source plus photos. Preserve source IDs, exact original locators, hashes, original text, media references and unknown timestamps. Keep stories separate from relationships. Unsupported chat ZIP/audio/video files remain stored but not analyzed; add a parser only for an available real sample if time permits. Do not spend this sprint on a universal importer or live archive crawl.

Export current accepted state and BookPassage data supplied by the lead into an illustrated English PDF plus ZIP containing project JSON, sources and original media. Use a prepared book layout; the lead supplies genuinely updated prose. The edited JSON must reopen in the application. Implement standalone editable HTML only after this route passes. Verify PDF opens, source links resolve, Unicode survives, ZIP contents exist and edit/reopen works. Push a callable first version by T+35, hand off early for T+55 integration, then fix only core defects. Keep all actual family inputs and generated private books out of GitHub; push code and sanitized test fixtures.
