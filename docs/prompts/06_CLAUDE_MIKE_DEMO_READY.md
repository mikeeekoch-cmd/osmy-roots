# Claude Code Mike: ingest the frozen packet and export the current book

Execute this assignment now in https://github.com/mikeeekoch-cmd/osmy-roots on codex/data-export in your own clone/worktree. Fetch and incorporate origin/codex/engineering, including baseline 556953f and the latest round-2 docs, preserving local work. Read AGENTS.md, docs/READINESS.md, docs/round-2/DEMO-READY-SCOPE.md, docs/round-2/NARRATIVE-DATA-CONTRACT.md and prompts 04-07. The latest 120-second/four-owner scope overrides old timing, text-only parser cuts and your previous ownership of private content creation.

You own server/ingestion, server/research, server/export, their tests and docs/status/data-export.md. Product owns actual packet files/facts/photo labels/translations/manifest values and the presenter script. Mike owns shared contracts/routes/state/dependencies and the strict runtime bundle adapter; Natalia owns UI. You are not alone in the codebase. Preserve their work, keep the stricter lead adapter, never force-push and ask Mike for dependencies instead of editing package/lockfiles. Follow the lead's T0+90 clock, contract and integration checkpoints.

1. Consume Product's private frozen packet or its public-safe fictional twin. Give Product exact supported input formats and parser findings promptly. Do not independently rewrite family facts or create a second artifact pack. Ask for the specific missing private handoff if unavailable and continue the same engineering work with the twin.
2. Implement bounded ingestion of the actual supplied one-page PDF, Family_Register.csv, Family_Relationships.csv, English TXT/captions, 6-10 photo files and three WhatsApp-style ZIPs. A normal intake must reconstruct the source-supported 35-person set without a manually uploaded internal project JSON. Preserve IDs, direction, uncertainty, source roots and the audited 57-to-56 relationship reconciliation. Robustly parse quoted CSV/newlines; validate missing/duplicate IDs and dangling links.
3. Actually parse the supplied PDF. If a prepared extraction sidecar is needed, hash-bind and label it; do not claim PDF extraction succeeded. Preserve original bytes and source locators. File support outside the selected formats may remain honestly unsupported.
4. Extract the exact supplied chat archives with UTF-8 _chat.txt, multiline messages and selected attachments. Preserve root hashes, message locators, English text, reconstruction metadata and missing-attachment outcomes. Synthetic speaker/time scaffolding does not change the real recollection's author/source. Repeated adaptations of one book passage share an evidence root.
5. Enforce archive limits with Mike: no traversal/absolute paths, symlinks, nested expansion or executable content; reject malformed/encrypted unsupported archives, cap entries at 200 and expanded bytes at 100 MB per run with a compression-ratio bound. The shared upload policy is 100 MB total, 25 MB per file, 40 files. Test a valid upload above the former 30 MB cap through the lead's integration.
6. Implement staged retrieval/source modules driven by the canonical manifest. Essential people/facts already exist in the initial files. Saved-folder/correspondence copies can add visible evidence without being counted as new independent sources or online discoveries. Return real source results and dependency-safe proposed batches for the lead's six releases at seconds 44/53/62/71/80/89. Do not use timers inside export or hardcoded family values.
7. Keep the key story source uploaded but unaccepted until the live Astra review. Presenter expected answers remain outside ingestion/model context. English derivatives cite exact English spans and retain lineage to private original hashes/locators. Preserve conflicts, approximate dates and supplied photo order; never infer faces.
8. Prepare an illustrated, readable four-page current PDF, longer HTML and complete portable project ZIP. Include Osmy Roots branding, dedication, a legible main branch and full selected family register, reviewed source-backed story, selected photos, source references and open questions as specified. The complete tree/photo/story collection stays in HTML/project. Prevent clipped names, overflow, raw JSON prose and missing citation targets.
9. All demo output text and filenames are English. Include English derivative sources and unchanged photo bytes under English filenames. Keep private Russian originals/audit notes outside the demo ZIP. Preserve the older general-purpose raw export/reopen behavior.
10. Support book/bundle preparation by second 108 and a real download click by 115, no later than 120. Coordinate version/hash keys with Mike so correction invalidates prepared output and Download serves exactly the sealed reviewed state. No fixed prior book as a substitute. Keep early download, failure/retry, ZIP integrity and portable reopen working.

Run baseline/new tests for exact packet formats, expansion limits, source-root deduplication, translations, photo hash/name mapping, same-name IDs, CSV references, missing attachments, current-state changes and English/raw export routes. Render every PDF page and inspect layout; verify actual ZIP/source/photo hashes and reopened state. Hand working functions to Mike before polish, then resolve integration failures.

Deliver safe commits, parser compatibility report for Product's exact packet version, timing measurements, export QA and remaining gaps. Product owns content-ready status; you own data/export-ready status; Mike owns integrated-demo-ready after two actual browser runs and the shared regression gate.
