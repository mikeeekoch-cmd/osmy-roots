# Claude Code Mike: round 2, English demo artifacts, ingestion and export

Work in https://github.com/mikeeekoch-cmd/osmy-roots on codex/data-export in your own clone/worktree. Execute this assignment when Mike launches this prompt. Fetch and incorporate current origin/codex/engineering, including code baseline 556953f or newer, preserving your work. Read AGENTS.md, docs/READINESS.md, docs/round-2/DEMO-READY-SCOPE.md and all round-2 prompts. The newer specification overrides the old decision to skip fixture chat parsing and the old mixed-language demo presentation.

You own server/ingestion, server/research, server/export, their tests, private demo-artefacts generation/scripts and docs/status/data-export.md. Mike owns contracts/routes/state/dependencies and the strict runtime bundle adapter; Natalia owns UI. You are not alone in the codebase. Preserve their changes, do not replace the lead's adapter with an older alternative, and never force-push. Request minimal dependencies from Mike without editing package/lockfiles yourself.

1. Build the actual private demo-artefacts folder from the selected Russian family book, existing source records and supplied photo labels. Use only the selected 35-person branch and needed source pages; do not recrawl the whole archive. Locate the ignored private-input starter in Mike's engineering clone or the existing local family_tree source workspace. If access is unavailable, ask for that specific path and continue with a clearly fictional schema-compatible twin. Never place real family contents or private paths/URLs in public Git.
2. Deliver 01-upload with 6-10 carefully selected original photos, a polished one-page English Family_Overview.pdf, English Family_Recollections.txt and three real ZIP files: WhatsApp_Mom.zip, WhatsApp_Dad.zip and WhatsApp_Family.zip. Target the complete upload selection at <=20 MB and <=20 files. Provide 00-START-HERE.md with the exact selection, expected total size and seven presenter answers. No internal project JSON, old full book or output archive belongs in this selection.
3. Rename photo copies to English names ordered left to right, supported by actual supplied annotations. Preserve original bytes and hashes. The ordered manifest maps asset/hash, position, person ID, source and confirmation state. Do not infer identities from appearance, a grave inscription or an ambiguous filename. Use Unknown or omit ambiguous group photos until clarified. Keep original private masters and a rename map outside the visible packet.
4. Translate real selected facts into English derivative sources with exact source/page/paragraph anchors and original hashes retained in a private provenance folder. Preserve conflicting dates, approximate years and uncertain relationships. Do not invent a migration, quote, relative or biographical detail. Each reconstructed chat sentence maps back to the actual supporting book/document span and evidence root. Simulated speakers/timestamps are format scaffolding, not proof that Mom or Dad sent a message; record reconstruction in file/source metadata. Avoid duplicated corroboration from three adaptations of the same fact.
5. Create 02-background with selected English saved-folder/correspondence materials and roughly eight staged source batches for the lead's 55-140 second map phase. Every proposed person/link/story must resolve to uploaded or staged evidence and stable IDs. Feed these through your callable retrieval/ingestion module; do not hardcode family data into React or pretend they are live web discoveries. Give Natalia only the fictional twin and schemas.
6. Implement bounded extraction for the supplied WhatsApp-style ZIPs: UTF-8 _chat.txt, message locators, selected attachments, missing-attachment outcomes and root archive hashes. Test the exact three generated archives through production ingestion. Reject traversal, absolute paths, symlinks, nested expansion, malformed/encrypted inputs and decompression bombs with the shared limits. Do not execute archive content. This validates the supported demo format, not universal native WhatsApp compatibility.
7. Make the supplied one-page PDF actually usable as source input through a bounded parser; if a prepared text-sidecar fallback is necessary, label and hash-bind it explicitly. Preserve TXT processing and unsupported-format honesty. Provide English outcomes and source titles; coordinate all DTO/dependency changes with Mike. The shared upload policy is 100 MB total/25 MB per file/40 files, with separate safe extraction caps.
8. Adapt the final illustrated PDF/HTML and project ZIP to the English demo projection. Keep original photo bytes with English filenames, an English source register and exact English derivative spans with translation lineage. Keep original Russian audit materials separate from the English demo download. Preserve general-purpose export/reopen for older raw projects. Fix source/page overflow and resolve every citation within the bundle; do not output raw JSON as book prose. Preserve the compact current-state chapter behavior and full editable project.

Verify all baseline data/retrieval/export tests plus the new pack, archive caps, source-root deduplication, translation/rename mapping, missing attachment, English-only archive text, ZIP integrity, asset hashes, current-state changes and portable reopen. Render and inspect every PDF page and the one-pager. Give Mike the actual private folder, safe generation commands, fixture-size report, staging manifest and source-backed seven-answer sheet. Hand off working functions and safe commits early, then support integration failures before polish. Report reconstructed/prepared/live work accurately. The product is demo-ready only after the lead completes the shared two-run browser gate.
