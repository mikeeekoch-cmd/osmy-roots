# Codex Natalia: intake, visible research and complete photo viewing

Execute round 3 in https://github.com/mikeeekoch-cmd/osmy-roots on the existing UI owner branch/worktree. Fetch origin, include fc93d39 or newer, preserve all existing work. Read AGENTS.md, docs/READINESS.md and docs/round-3/README.md, SPEC.md and COORDINATION.md. Consume Mike's actual shared contracts. The latest flow uses six initial checks, an explicit Start research and two Research deeper rounds, with about 20 seconds of visible actual work before progressive results. The old total 120-second ceiling is superseded; no visible countdown is requested.

You own src/ui, UI brand assets, tests/ui and UI status. Mike owns contracts/state/Astra/provider authorization/API/dependencies. Claude owns parsers, retrieval, private family content, all enhanced assets and the full book. You are not alone; preserve other edits and work with one manifest. Do not invent private family data, backend truth or a second contract.

1. Remove the entire group-photo left-to-right/Unknown instruction, the memories-versus-archives footer, Other names or spellings and Current place. Put full name plus optional birthplace/birth date in the main section. Add optional father/mother/grandfather/grandmother cards with name/place/year; grandparent side may remain unknown. Optional blank fields must not block Continue.
2. Put one Fill my family details button below the optional section. Render Mike's source-backed draft/autofill results, conflicts and source details; preserve manual edits and leave missing facts blank. It must work in one click for known fields and remain editable. Do not silently start research or hardcode presenter values.
3. Replace generic saved-copy tiles with official recognizable Drive/Gmail marks and concise connection controls. Render Connected only from Mike's server-verified state. Include meaningful disconnected/connecting/expired/error behavior and account details. Manual files do not set Connected; a fake successful login is unacceptable for the requested demo claim.
4. Render exactly six initial checks from the API. At least three distinct questions have large useful photos: modern family, old portrait and old group scene selected by Claude. Replace the current 180px-only image with responsive large presentation and full-view/zoom. Keep source evidence and explicit confirm/correct/unknown/skip concise. Later research questions belong in a separate persistent question bank, not in the initial question count.
5. Workspace entry supports explicit Start research. After initial completion offer Research deeper; after that cycle offer the second deeper action. Active work cannot be duplicated by repeated clicks. Refresh resumes the same cycle. Show immediate Now/Next tasks and gradually arriving actual results; do not wait behind an opaque spinner or animate timer-derived progress.
6. Replace the newest-first event dump with active/next work and a chronological completed history grouped by research round. Use job identity so running/completed states of one job do not look simultaneous. Show what Astra is checking, which source/tool is active, useful result summaries and upcoming tasks. Surface blocked/no-match outcomes plainly.
7. Render Mike's authoritative source/photo/evidence/search/people metrics with clear units, totals and per-round changes. Include photos received/indexed and enhancements ready. Do not reset completed intake totals or pad counts. Keep candidate/reviewed and cached/live distinctions accessible. A real zero remains accurate; planned tasks can be presented as planned rather than implying completed websites.
8. Remove the large bottom book-ready panel and duplicate Download. Put compact book status/info beside the primary Download button. It opens page count/current-edition details and the actual full PDF preview while preserving map space, preparing/stale/error/retry states and accessible keyboard behavior.
9. Make every linked photo/document reachable from each person's card, including caption-associated originals whose identity is still unreviewed. Use reviewed unique solo portraits or explicit person crop metadata where available. Avoid assigning the same group picture as an apparent solo portrait to several relatives. Missing supported portraits can use initials; never infer faces.
10. Reproduce the user's broken full-photo case before claiming a cause. Inspect all actual originals and data responses. Repair the inline lightbox/scroll/stacking/error handling as needed; scope the broad .photo-lightbox button absolute-position rule so it cannot overlap comparator controls. Add explicit retry and correct reset across person/image changes. Test desktop and narrow/mobile layouts.
11. Every selected old photo must have Claude's aligned original/enhanced pair and a working slider in full view. Modern photos retain ordinary viewing. Verify original/50-50/enhanced extremes, mouse/touch/arrows/Home/End, focus trap/Escape/restoration, gallery-arrow isolation and stable crop/aspect. A missing/unusable pair remains incomplete; do not substitute a stretched or unrelated image to satisfy the check.

Start layout work with clearly fictional fixtures while contracts/content are published. Integrate the actual API early. Preserve raw source text, uncertainty and all previous review/edit/undo behavior. Avoid a third main product screen; use the existing workspace, dialogs and drawers.

Run focused UI/state checks plus typecheck and relevant build checks. Provide a per-photo test matrix and actual browser evidence for all selected originals/pairs, six checks, two deeper buttons, question bank, compact book controls and refresh. The lead owns final two complete rehearsals; your isolated UI test cannot declare the app ready. Commit only owned files and update UI status with exact working, missing-asset and API dependencies. Push safe UI code; no private screenshots, photos, names or tokens enter public Git.
