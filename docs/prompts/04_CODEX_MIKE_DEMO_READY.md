# Codex Mike: round 2, orchestration and demo-ready integration

Work in https://github.com/mikeeekoch-cmd/osmy-roots on codex/engineering. Execute this assignment when Mike launches this prompt. Fetch origin and preserve local work. Include baseline 556953f or a descendant from origin/codex/engineering; main alone was last verified at a5de7c4. Read AGENTS.md, docs/READINESS.md, docs/round-2/DEMO-READY-SCOPE.md and the two other round-2 prompts. The new scope controls this round where it differs from old intake/language/timing instructions.

The current P0 and partial P1 passed 74 tests, typecheck and build; preserve them. Your goal is the full integrated three-minute Osmy Roots demo described in the shared specification. A loader over the current one-person graph does not satisfy it. The current Astra analysis only chooses existing people; extend the actual data flow to propose and persist new people, relationships and stories from supported text.

You own packages/contracts, server/agent, server/state, server/events, src/app, dependency/config/lockfiles, integration-tests and lead status. Natalia owns UI and logo; Claude owns ingestion/research/export and private artifact generation. You are not alone in the codebase. Preserve their changes, use their handoffs and never reset or force-push shared work. Do not rewrite worker modules while they are building.

1. At execution start, record a new round-2 T0 and checkpoints. The three-minute constraint is demo runtime. Publish shared Zod/types, example payloads and API methods within ten minutes: resumable run phases/sequences, seven setup questions/answers, ordered photo-person annotations, proposed graph batches, English derivative/source lineage, real versus saved connections, staged job eligibility and export sealing. Keep roots-v1 reopening compatible. Freeze interfaces once published and tell workers the commit through shared status.
2. Implement a files-only start that returns a persisted preparation state promptly. No source jobs or timers before Start, no blank graph flash. Run actual bounded parsing and Astra work. Generate seven varied source-backed questions with Recommended, editable answers and unknown. Confirming must save exactly what the question asks. Keep same-name candidates separate and photo identity based on supplied labels, never appearance.
3. Implement staged source processing and actual saved graph/evidence batches across the shared 0-180 second script. The server owns order, eligibility, versions and deduplication; the UI animates real deltas. Use roughly eight map-phase arrivals. Do not expose an already-complete graph under a cosmetic timer. Existing book facts retain prepared origin; only real requests are labelled Astra analyzing. No fake website discoveries or endless model activity when idle.
4. Integrate Claude's English photo/document/chat pack. An ordinary intake must work with the supplied files without uploading a prepared project JSON. Preserve raw private Russian evidence outside the English demo project; validate exact quotations against the English derivative plus recorded lineage. Apply English-only names, source summaries, errors and generated book output. Do not overwrite private originals or reclassify reconstructed dialogue as authentic family messages.
5. Publish and enforce shared upload limits: 100 MB total, 25 MB per file, 40 files, bounded multipart body, plus Claude's archive expansion limits. Integrate client preflight with useful errors. Prove a valid request over 30 MB now works. Keep credentials server-side and localhost protections intact.
6. Preserve mid-run contribution, correction, unknown, undo and conflict checks. Refresh resumes the same cursor. Stop/cancel/new-project isolates old work. A user correction must beat a stale model result. When no jobs run, show the truthful review/ready state instead of fabricated ongoing inference.
7. Download seals the current run, stops queued additions, handles in-flight work safely, generates a current English cited passage and calls the real exporter for one consistent version. Mark completed only after successful output; make failure/retry and early download work. A completed run cannot receive delayed graph updates.
8. Integrate Natalia and Claude early, then execute every gate in the shared specification. Run all baseline/new tests, typecheck/build, English scans, actual ZIP/asset/reopen checks, all-page PDF visual review and two real-browser Start-to-download rehearsals under 180 seconds. At least one new clue must go through a real Astra call and review, not a canned answer. Record exact timing, failed checks and prepared versus live steps.

Deliver the running local URL, tested commit, private input folder and one-page English presenter instructions with seven answers. Update readiness/build log/lead status and push safe code on the engineering branch. Follow the applicable default-branch approval restriction for final publication. Do not claim demo-ready until the full integrated release gate passes. Continue fixing blockers; do not stop at contracts, a plan or isolated tests.
