Repository: https://github.com/mikeeekoch-cmd/osmy-roots

We are building Roots with three concurrent workers: Codex on Mike's account, Codex on Natalia's account/other laptop, and Claude Code on Mike's account. The goal is a convincing LOCAL prototype within 90 minutes of the lead's recorded T0. We cannot deliver a full production app in this time. Make documented scope tradeoffs to protect the demonstrated route. Mike is here to help; ask a concise question for missing input, credentials or a blocker over five minutes and keep doing independent work.

Start by fetching the repository and reading AGENTS.md, docs/EXECUTION.md, docs/PRODUCT.md, docs/ARCHITECTURE.md, docs/CONTRACT-V3.json, docs/READINESS.md and docs/status/*.md. Read the latest private scope/engineering tab and HTML reference available through Mike's configured private context or local download. Report inaccessible references; the safe repository scope is sufficient to start independent work. The current 90-minute plan supersedes V3 staffing and full-platform dependencies. GitHub is the engineering ground truth: commit/push your safe code, tests, decisions and owner status at meaningful checkpoints, about every 15 minutes. Routine commits/pushes are authorized. Private family assets, Drive URLs, credentials and raw model traces stay outside this public repo.

You are not alone in the codebase. Use your assigned branch in a separate clone/worktree, preserve others' changes and never force-push/reset shared work. The lead owns contracts, dependency/lockfiles and integration. Hand off branch + commit + commands + checks + remaining gaps. All workers use the lead's shared T0+90 deadline. Start work now; do not stop after proposing another plan.

You are Claude Code Mike, the product UI engineer. Work on codex/ui in a separate worktree. Own src/ui and its UI tests plus docs/status/ui.md. Preserve existing repository setup. Do not modify backend, src/app wrappers, shared schemas or dependency/lockfiles without the lead; report needed libraries and interface changes.

Build the two-screen experience from docs/PRODUCT.md and the latest private HTML reference. Start the layout immediately with a labeled fixture adapter while the lead freezes shared contracts in the first ten minutes. Keep the UI adapter replaceable; connect it to real run/state/review/export interfaces as soon as the lead publishes them.

Screen 1: compact drag/drop; starting name, family geography or explicit unknown, and files/context are required. Optional place/time/name details collapse naturally. Connections are separate previews. Start the search opens Screen 2 and triggers internal planning with no approval page.

Screen 2: concise progress/findings left, an editable generational map center, visually distinct human Q&A right. Use the supplied focused family branch with English names/life years, person photos/gallery, clickable people and relationships, evidence, uncertainty/discrepancies and history. Let the user add a clue and accept/correct/choose unknown; the visible graph must reflect saved state. One Download family book action calls Natalia's integrated export. No separate plan, book or build page. A fixed generational layout is an acceptable time-saving fallback; preserve inspectability and original photo proportions.

Push a usable shell by T+25 and a connected first version by T+45. Support lead integration by T+55. After T+70 prioritize blockers, legibility and the three-minute demo. Verify the actual desktop browser when available and report limits honestly. Keep placeholders distinct from real progress and do not copy prior mockup/ArtLens implementation into event work. Record screenshots only with synthetic/public-safe data in Git; private family screenshots stay private.
