# Codex Mike: real research cycles and integration

Execute round 3 in https://github.com/mikeeekoch-cmd/osmy-roots on codex/engineering. Fetch origin, preserve all existing work and include fc93d39 or newer. Read AGENTS.md, docs/READINESS.md, docs/round-3/README.md, SPEC.md and COORDINATION.md. They govern the latest six-check, three-cycle, full-book experience. The previous 120-second total, fixed batches, four-page book and four-owner arrangement are superseded. Do not treat old acceptance as proof of this revision.

You own packages/contracts, server/agent, server/state, server/events, new server/connectors authorization, src/app routes/adapters, dependencies/config, integration-tests, scripts/browser-rehearsal.mjs and lead acceptance/status. Natalia owns UI. Claude owns ingestion/retrieval/export plus private content, photo preparation, book adaptation and the public round-3 fictional packet. You are not alone; preserve other edits and owner history, avoid resets/force-pushes and use one shared manifest. Do not create a fourth content agent.

1. Record actual execution start and a realistic delivery estimate after checking credentials and asset readiness. Publish actual versioned types/API examples within ten minutes. Preserve earlier project/packet compatibility. Deliver the contract rows in COORDINATION.md, not an alternative private schema. The old engineering deadline is historical.
2. Implement optional self birth place/date and four relative profiles. One explicit Fill my family details action creates/applies source-backed draft values from selected files/private profile, preserving manual edits and unknowns. No hidden personal values in public code and no background research before an explicit action.
3. Implement real read-only Google Drive/Gmail authorization and selected-source imports with Claude. Expose verified connected/expired/error states only after actual authorized reads. Keep credentials server-side and out of portable exports. A Codex connector or uploaded copy does not prove an application connection. Inspect current official provider documentation at implementation time; request only missing concrete access when needed, without repeating already granted authorization.
4. Implement Continue as explicit persisted intake preparation: parse the files, create exactly six initial checks and run the logged Astra recollection check in a separate intake-job stage. Autofill never invokes Astra. Keep the initial checks separate from the persistent later question bank. Preserve source spans, explicit confirm/correct/unknown/skip, idempotency and refresh. The actual recollection interpretation precedes acceptance without presenter answers or prepared identity hints in model input. Reuse the source-matched speaker fix.
5. Implement Start research after the initial checks, then two distinct Research deeper rounds on explicit clicks. Enforce one initial plus at most two deeper cycles server-side; retry stays in the same cycle and duplicate/third-deeper requests cannot create extra work. Persist the Astra plan, objectives, jobs, source scope, actual tool outcomes, cycle status and version. Each click shows useful activity immediately; target first validated results around 20 seconds, then progressive saved results. No fake minimum delay, timer-generated counters or automatic next round on refresh.
6. Execute Claude's real local/authorized-provider/public-search/crawl adapters. Use bounded jobs, leases, cancellation, duplicate suppression, retry and explicit no-match/blocked/timeout outcomes. Expose short user-readable work summaries and next actions, without hidden model reasoning. Keep prepared family records and cached material labeled in source/job provenance.
7. Extend existing-person-only model proposals to source-backed new-to-project people and relationships. Validate exact citations, same-name alternatives, cycles, identities and evidence-root duplication. Use atomic reviewed graph changes; do not silently accept an internet name match. Queue lower-priority questions, apply later answers, update dependent state and invalidate affected book sections.
8. Centralize meaningful metrics from persisted outcomes. Count received/processed files, documents, original photos, indexed/enhanced photos, evidence checks, actual Astra analyses, searches, unique retrieved pages, candidates and reviewed additions separately. Carry intake work forward. No fabricated nonzero counters, derivative-as-original inflation or repeated-copy corroboration.
9. Integrate the BookPlan and 35-40-page BookEdition. Remove the four-page/100-word bottleneck as coordinated with Claude. Cache prepared source-backed chapters and regenerate only affected current sections. Preview and ZIP must share one edition fingerprint. Preserve immutable sealing/retry; continuing after a downloaded edition creates an editable successor.
10. Tighten pair validation across intake, staging, edits and portable reopen. Pair availability must never confirm a face association. Preserve original/derivative roles, parent hashes and one evidence root. Remove the current automatic portrait attachment when a pair has no reviewed photo answer.

Integrate continuously. Keep the prior working runtime available until the new candidate passes. Run all baseline/new meaningful tests, typecheck and production build. Require real successful selected-provider reads, actual query/retrieval execution, six checks/three photo questions, all three cycles, a later bank answer, source-backed graph changes, honest metric reconciliation, every old-photo slider and a current 35-40-page book. Exercise no-match, expired auth, duplicate clicks, stale jobs, refresh, cancel and export retry.

Require two actual browser rehearsals on the same final app and frozen packet; one refreshes/resumes. Each includes Start plus two deeper clicks, source/review changes, photo comparison, full current-PDF preview and actual ZIP receipt. Measure first useful activity/results for each cycle and total time; the former 120-second ceiling no longer applies. Claude must inspect every PDF page and verify all original/derivative hashes and empty-store reopen. Never declare this revision ready from old recordings, a static book or fixture-only tests.

Update docs/READINESS.md, BUILD-LOG, round-3 acceptance and lead status with exact commits, timing, prepared/live distinctions and concrete remaining blockers. Push safe code/status to engineering. Private family material remains ignored/local; default-branch publication keeps its existing separate restriction. Deliver the running URL, tested commit, exact private input/output paths and revised English presenter cues. Existing scoped Astra approval persists; it does not cover unrelated new photo/book transmissions or replace actual OAuth access.
